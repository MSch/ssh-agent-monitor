[Unit]
Description=SSH Auth Socket manager
After=basic.target
RequiresMountsFor=/run/user/%U/ssh-auth-sock

[Service]
Type=simple
ExecStart=@BINDIR@/ssh-agent-monitor
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
